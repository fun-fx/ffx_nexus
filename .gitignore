# Go
/bin/
/nexus
*.test
*.out

# Node / dashboard
web/node_modules/
# web/dist is committed: it is embedded into the Go binary (web/embed.go) so the
# console can serve the dashboard. Rebuild with `npm --prefix web run build`.

# Python / eval-service
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# Env / secrets
.env
.env.local

# OS
.DS_Store
mock-upstream
/bin/
