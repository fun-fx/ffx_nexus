.DS_Store
.git/
.gitignore
*.tmproj
*.bak
*.tgz
.idea/
.vscode/
ci/
# Examples live in templates/eval-plugins/ but are not referenced
# by any active template; helm 3.17's chart layer scans .yaml files
# in templates/ and tries to parse them as templates, which fails
# on the literal `\n` escapes used in pipeline traces. Exclude.
templates/eval-plugins/
