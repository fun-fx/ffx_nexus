apiVersion: v2
name: nexus
description: Observability-first, OpenAI-compatible LLM gateway (open-core).
type: application
# Chart version; bump on chart changes.
version: 0.3.2
# Default upstream app version; overridable via image.tag.
appVersion: "0.3.2"
home: https://github.com/fun-fx/ffx_nexus
sources:
  - https://github.com/fun-fx/ffx_nexus
keywords:
  - llm
  - gateway
  - openai
  - observability
  - proxy
maintainers:
  - name: ffx-nexus
